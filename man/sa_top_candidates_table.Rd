% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-report.R
\name{sa_top_candidates_table}
\alias{sa_top_candidates_table}
\title{Top candidates table (HTML tag)}
\usage{
sa_top_candidates_table(res, current_model = NULL, y = NULL, n = 5)
}
\arguments{
\item{res}{Result of \code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}}.}

\item{current_model}{Optional fitted \link[seasonal:seas]{seasonal::seas} model to mark as "current".}

\item{y}{Optional original series (needed to compute a few current-model diagnostics).}

\item{n}{Number of top rows to display (default 5). The airline/current rows
may be appended even if not in the top \code{n}.}
}
\value{
An \code{htmltools} tag (\verb{<table>}) you can insert into reports.
}
\description{
Build a colour-coded HTML table of the highest-ranked candidate models,
always placing the \strong{best} model at the top and \strong{including the current
model} (if supplied) even if it is not in the top \code{n}. The airline
reference model ARIMA (0 1 1)(0 1 1) is also appended if absent.
}
\details{
Rows are lightly shaded: ✓ best (green), ★ current (blue), airline (red).
}
