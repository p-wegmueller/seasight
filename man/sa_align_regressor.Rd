% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz-core-robustness.R
\name{sa_align_regressor}
\alias{sa_align_regressor}
\title{Align an external regressor to a target series}
\usage{
sa_align_regressor(y, x)
}
\arguments{
\item{y}{Target series, a \code{ts} or object convertible with \code{tsbox::ts_ts()}.}

\item{x}{Regressor series, a \code{ts} or object convertible with \code{tsbox::ts_ts()}.}
}
\value{
A \code{ts}/\code{mts} aligned to \code{y}, or \code{NULL} if conversion, frequency or
span alignment fails.
}
\description{
Converts \code{x} to a base \code{ts}, checks that it has the same frequency as \code{y},
windows it to the span of \code{y}, and returns a \code{ts}/\code{mts} with the same start,
end and frequency as \code{y}. Returns \code{NULL} rather than throwing when alignment
is impossible.
}
