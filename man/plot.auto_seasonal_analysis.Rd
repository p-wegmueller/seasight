% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.auto_seasonal_analysis}
\alias{plot.auto_seasonal_analysis}
\title{Plot an automatic seasonal-analysis result}
\usage{
\method{plot}{auto_seasonal_analysis}(
  x,
  series = c("final", "seasonal", "original"),
  xlab = "time",
  ylab = NULL,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}}.}

\item{series}{Which series to show. \code{"final"} plots the selected seasonally
adjusted series when available and falls back to the original series for
do-not-adjust cases. \code{"seasonal"} plots the selected seasonal component
when available. \code{"original"} plots the original input series stored in the
result.}

\item{xlab, ylab}{Axis labels.}

\item{...}{Additional arguments passed to \code{\link[stats:plot.ts]{stats::plot.ts()}}.}
}
\value{
Invisibly, \code{x}.
}
\description{
Provides a compact default plot for the main result class returned by
\code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}}. The plot is intended for quick inspection; the
HTML report remains the richer review artifact.
}
\examples{
\donttest{
if (requireNamespace("seasonal", quietly = TRUE)) {
  res <- auto_seasonal_analysis(AirPassengers, max_specs = 3)
  plot(res)
}
}
}
