% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa_helpers.R
\name{build_user_xreg}
\alias{build_user_xreg}
\title{Build user-supplied calendar regressors (Genhol-style)}
\usage{
build_user_xreg(
  y,
  td_candidates = NULL,
  holidays = NULL,
  frequency = NULL,
  td_usertype = "holiday",
  default_center = c("calendar", "mean", "none")
)

build_user_xreg(
  y,
  td_candidates = NULL,
  holidays = NULL,
  frequency = NULL,
  td_usertype = "holiday",
  default_center = c("calendar", "mean", "none")
)
}
\arguments{
\item{y}{A \code{ts} (or ts-boxable) target series used for alignment.}

\item{td_candidates}{Optional named list of precomputed regressors (ts-boxable).}

\item{holidays}{Optional list defining moving-holiday regressors. Each element may be:
\itemize{
\item a \code{Date} vector (holiday dates), or
\item a list with fields \code{dates} (Date), \code{start} (int), \code{end} (int), \code{center} (chr), \code{name} (chr)
Windows are in days relative to the holiday date. Defaults: \code{start=-7}, \code{end=0}.
}}

\item{frequency}{Optional frequency to use for holiday regressors (defaults to \code{frequency(y)}).}

\item{td_usertype}{X-13 usertype label for \emph{all} returned regressors (default \code{"holiday"}).}

\item{default_center}{Default centering for holiday regressors (default \code{"calendar"}).}
}
\value{
A named list of \code{ts} regressors aligned to \code{y}. Attribute \code{td_usertype}
is attached for downstream use.

A named list of \code{ts} regressors aligned to \code{y}. Attribute \code{td_usertype}
is attached for downstream use.
}
\description{
Helper to create \emph{generic} calendar regressors without relying on any
country-specific datasets shipped with the package.

This helper lets users provide \emph{generic} calendar regressors without relying
on any country-specific datasets shipped with the package.
}
\details{
Supports:
\itemize{
\item Precomputed regressors (\code{td_candidates}): named list of ts-boxable series
\item Moving-holiday regressors (\code{holidays}): built via \code{\link[seasonal:genhol]{seasonal::genhol()}}
}

The output is directly consumable by \code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}} via its
\code{td_candidates} argument.

Supports:
\itemize{
\item Precomputed regressors (\code{td_candidates}): named list of \code{ts}-boxable series
\item Moving-holiday regressors (\code{holidays}): built via \code{\link[seasonal:genhol]{seasonal::genhol()}}
}

The output is directly consumable by \code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}} via its
\code{td_candidates} argument.
}
