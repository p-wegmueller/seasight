% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-batch.R
\name{sa_batch_run}
\alias{sa_batch_run}
\title{Run seasonal-adjustment reports over a list of series}
\usage{
sa_batch_run(
  series,
  out_dir = "sa_reports",
  ids = NULL,
  resume = TRUE,
  timeout = Inf,
  report_fun = sa_report_html,
  log_file = NULL,
  ...
)
}
\arguments{
\item{series}{A \code{ts} object or a non-empty list of series.}

\item{out_dir}{Directory where HTML reports and \verb{_sa_run_log.csv} are written.}

\item{ids}{Optional character vector of IDs, same length as \code{series}.}

\item{resume}{If \code{TRUE}, existing output files are skipped and logged as \code{skipped}.}

\item{timeout}{Per-series timeout in seconds. Use \code{Inf} to run in the current
R process without a timeout. Finite values require the suggested \code{callr}
package and run each case in a clean subprocess.}

\item{report_fun}{Report function to call per item. Defaults to \code{\link[=sa_report_html]{sa_report_html()}}.}

\item{log_file}{Optional CSV log path. Defaults to \verb{_sa_run_log.csv} in \code{out_dir}.}

\item{...}{Additional arguments passed to \code{report_fun}.}
}
\value{
A tibble with one row per series: \code{id}, \code{ok}, \code{status}, \code{reason},
\code{message}, \code{outfile}, and \code{elapsed_seconds}.
}
\description{
Sequential batch runner with per-series status rows and a structured CSV log.
Existing outputs can be skipped via \code{resume = TRUE}.
}
