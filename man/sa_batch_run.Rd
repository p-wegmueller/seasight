% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-batch.R
\name{sa_batch_run}
\alias{sa_batch_run}
\title{Run seasonal-adjustment reports over a list of series}
\usage{
sa_batch_run(
  series,
  out_dir = "sa_reports",
  ids = NULL,
  timeout = 300,
  resume = TRUE,
  report_fun = sa_report_html,
  log_file = NULL,
  ...
)
}
\arguments{
\item{series}{A \code{ts} object or list of series. List names are used as IDs.}

\item{out_dir}{Directory where HTML reports and \code{_sa_run_log.csv} are written.}

\item{ids}{Optional character vector of IDs, same length as \code{series}.}

\item{timeout}{Per-series timeout in seconds. Use \code{Inf} or \code{NULL} for no timeout.}

\item{resume}{If \code{TRUE}, existing output files are skipped and logged as \code{skipped}.}

\item{report_fun}{Report function. Defaults to \code{sa_report_html()}. Primarily useful for tests or custom report wrappers.}

\item{log_file}{Optional CSV log path. Defaults to \code{_sa_run_log.csv} in \code{out_dir}.}

\item{...}{Additional arguments passed to \code{report_fun}.}
}
\value{
A tibble with one row per series: \code{id}, \code{ok}, \code{status}, \code{reason},
\code{message}, \code{outfile} and \code{elapsed_seconds}.
}
\description{
Sequential batch runner with per-series status rows, resume behavior and a
structured CSV log. If \code{callr} is installed and \code{report_fun} is the default
\code{sa_report_html()}, each report is executed in a subprocess with the requested
timeout. Otherwise, the runner falls back to a local fail-soft call with a
base R elapsed-time limit.
}
