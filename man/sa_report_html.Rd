% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-report.R
\name{sa_report_html}
\alias{sa_report_html}
\title{Seasonal Adjustment: full HTML report}
\usage{
sa_report_html(
  y,
  current_model = NULL,
  td_usertype = "td",
  td_candidates = NULL,
  use_fivebest = TRUE,
  title = "Seasonal Adjustment Report",
  outfile = "sa_report.html",
  file = NULL,
  png_width = 1400,
  png_height = 900,
  print_to_console = FALSE,
  print_which = c("new", "current", "both"),
  include_easter = c("auto", "always", "off"),
  easter_len = 15L,
  engine = c("seats", "x11", "auto"),
  w_engine = 1,
  outlier_types = c("AO", "LS", "TC"),
  outlier_method = "AddOne",
  outlier_critical = 4,
  outlier_alpha = NULL
)
}
\arguments{
\item{y}{Time series to be analysed. Can be a \code{ts} object or anything that
\code{tsbox::ts_ts()} can convert to \code{ts}.}

\item{current_model}{Optional incumbent \code{\link[seasonal:seas]{seasonal::seas()}} model to compare
against the newly selected specification.}

\item{td_usertype}{Character string passed as \code{regression.usertype} when
trading-day regressors are used (default \code{"td"}).}

\item{td_candidates}{Optional named list of trading-day candidate regressors
(e.g. \code{list(wd = wd.m, wd1 = wd1.m)}), each aligned with \code{y}.}

\item{use_fivebest}{Logical. If \code{TRUE}, include the "five best" automatic
specs in the candidate grid.}

\item{title}{Title of the report shown in the HTML page.}

\item{outfile}{Path to the HTML file to be written.}

\item{png_width, png_height}{Width and height (in pixels) of PNG plots
embedded in the report.}

\item{print_to_console}{Logical. If \code{TRUE}, do not create an HTML file but
print the copy-pasteable model code to the console and return it.}

\item{print_which}{Which code blocks to print when \code{print_to_console = TRUE}.
One of \code{"new"}, \code{"current"} or \code{"both"}.}

\item{include_easter}{Controls inclusion of Easter regressors:
\code{"auto"} (default) lets the selector decide, \code{"always"} always includes
Easter, \code{"off"} never includes Easter. A logical value is also accepted
and mapped to \code{"auto"}/\code{"off"}.}

\item{easter_len}{Integer, length (in days) of the Easter effect when
included.}

\item{engine}{Preferred decomposition engine for candidate models.
One of \code{"seats"}, \code{"x11"} or \code{"auto"}.}

\item{w_engine}{Numeric weight for the engine choice component in the
composite ranking score.}

\item{outlier_types}{Character vector of outlier types to detect, typically
a subset of \code{c("AO", "LS", "TC")}.}

\item{outlier_method}{Character scalar giving the outlier detection method
passed to \code{seasonal::seas()} (e.g. \code{"AddOne"}).}

\item{outlier_critical}{Numeric critical value for outlier detection
(e.g. a t-/z-threshold).}

\item{outlier_alpha}{Optional numeric significance level; if supplied, it is
converted to a two-sided normal cutoff for \code{outlier_critical}.}
}
\value{
Invisibly, a list with elements \code{report} (path to the HTML file) and
\code{res} (the corresponding \code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}} result).
}
\description{
User-facing wrapper that runs the automatic seasonal analysis and writes a
human-readable HTML report with plots, tables, diagnostics, and
copy-pasteable \code{seas()} calls.
}
\details{
Notes:
\itemize{
\item If the provided \emph{current} specification equals the selected \emph{best} model,
the report omits the "Alternative model" comparison section.
\item The "Top candidates" table starts with the best model and always includes
the current model (flagged with a star ★) when one is supplied.
}
}
\examples{
\dontshow{if (requireNamespace("seasonal", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# sa_report_html(y = AirPassengers, outfile = tempfile(fileext = ".html"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=auto_seasonal_analysis]{auto_seasonal_analysis()}}, \code{\link[=sa_existence_card]{sa_existence_card()}}, \code{\link[=sa_top_candidates_table]{sa_top_candidates_table()}}
}
